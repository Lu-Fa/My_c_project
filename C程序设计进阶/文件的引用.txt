#include<stdio.h>
#include"MIN.h"
#include"minium.h"
//头文件相当于一个协议，里面存放函数原型和全局变量，避免引用错误
//声明是不产生代码的东西
//eg:函数原型，变量声明，结构声明，宏声明，inline函数
//而定义是产生代码的东西
//main()函数太大于是分成多个函数
//源文件太长于是分成多个源文件
//一个.c文件是一个编译单元，编译器每次只能处理一个编译单元
//编译是对单个源文件编译，构建（build)是对整个项目做链接
//#include是把那个文件里的全部文本内容原封不动的插入到它所在的地方
//通过条件编译和宏，确保#include头文件里的声明不被引用多次，避免重复声明
int main()
{
	int a = 9;
	int b = 5;
	printf("%d", MIN(a, gall));
	return 0;
}